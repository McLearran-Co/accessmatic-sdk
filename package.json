{
  "name": "accessmatic-sdk",
  "version": "1.0.0",
  "description": "Client SDK for automatic PDF accessibility",
  "main": "dist/accessmatic.min.js",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w"
  },
  "devDependencies": {
    "rollup": "^4.0.0",
    "@rollup/plugin-terser": "^0.4.0"
  }
}
